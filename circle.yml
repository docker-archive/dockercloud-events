machine:
  services:
    - docker
deployment:
  staging:
    branch: staging
    commands:
      - ./build.sh
      - ./deploy.sh tutum/events:staging
  master:
    branch: master
    commands:
      - ./build.sh
      - ./deploy.sh tutum/events:latest
